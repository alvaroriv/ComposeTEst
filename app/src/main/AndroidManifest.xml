<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".base.ApplicationClass"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Androidtemplate"
        tools:targetApi="31">
        <activity
            android:name="com.koombea.presenter.ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AndroidTemplate">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.koombea.presenter.ui.home.AddTransactionActivity">
        </activity>
        <activity
            android:name="com.koombea.presenter.ui.home.DashboardActivity">
        </activity>
        <activity
    android:name="com.koombea.presenter.ui.signup.SignUpActivity">
        </activity>
        <activity
        android:name="com.koombea.presenter.ui.settings.CurrencyActivity">
        </activity>
    </application>

</manifest>